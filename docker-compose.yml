version: '3'
services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    image: webapp
    ports:
      - "8080:8080"

    # Remove me when ubuntu 14.04 is less shitty
    network_mode: "host"
